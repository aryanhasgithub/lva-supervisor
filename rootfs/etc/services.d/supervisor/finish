#!/usr/bin/env bashio

if [[ "$1" -eq 0 ]]; then
  bashio::log.info "lva-supervisor exited cleanly, restarting in place"
else
  bashio::log.warning "Halting lva-supervisor (exit code $1)"
  /run/s6/basedir/bin/halt
fi