[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lva-supervisor"
version = "0.0.1"
description = "Supervisor for managing LVA-OS containers"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["lva_supervisor*"]